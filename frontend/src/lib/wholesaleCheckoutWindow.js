/**
 * Wholesale checkout window after the group fills.
 * Keep in sync with backend `WholesaleReservationLifecycleService::CHECKOUT_WINDOW_MINUTES`.
 */export const WHOLESALE_CHECKOUT_WINDOW_MINUTES = 180

export const WHOLESALE_CHECKOUT_WINDOW_HOURS = WHOLESALE_CHECKOUT_WINDOW_MINUTES / 60
