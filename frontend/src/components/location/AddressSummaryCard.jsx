import { LocationSelectedBar } from "@/components/location/LocationSelectedBar"

/** @deprecated Use LocationSelectedBar — kept for imports. */
export function AddressSummaryCard(props) {
  return <LocationSelectedBar {...props} />
}
