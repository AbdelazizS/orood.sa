# Server: cp .env.production.example .env.production && npm run build
# Leave VITE_API_URL empty — same host uses /api/v1 (no localhost in production).

VITE_USE_DEMO=false
VITE_API_URL=
VITE_SITE_URL=https://www.arooth.com
