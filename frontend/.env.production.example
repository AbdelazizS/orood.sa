# Copy on server: cp .env.production.example .env.production
# Use http:// URLs until SSL is active, then switch to https:// and rebuild.

VITE_USE_DEMO=false
VITE_API_URL=https://arooth.com/api/v1
VITE_SITE_URL=https://www.arooth.com
